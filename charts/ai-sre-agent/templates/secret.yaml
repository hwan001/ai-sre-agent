# prettier-ignore
---
{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-sre-agent.fullname" . }}
type: Opaque
stringData:
{{- range $key, $value := .Values.secrets }}
  {{- if $value }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
{{- end }}
